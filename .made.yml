version: 1
agent: codex
review:
  required: true
ci:
  required: true
commands:
  test: |
    set -e
    go build ./...
    go test ./...
    go test -race -shuffle=on -count=1 ./...
    go vet ./...
  lint: golangci-lint run ./...
